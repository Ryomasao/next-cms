---
title: 正規表現メモ
time: 2018-07-06 16:28
---

ファイル名のチェック

```
if(!preg_match('/^[a-zA-Z0-9-_\.]+$/', $fileName)){
            abort(404);
        }
```

- ^行頭から、$行末まで
- 内の文字が一文字以上の繰り返し +
- 内は許容する文字

preg\_matchは、マッチしたら1を、しなかったら0を返す。 if()は1はtrue、0はfalse。
